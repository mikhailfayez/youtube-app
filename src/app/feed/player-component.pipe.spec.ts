import { PlayerComponentPipe } from './player-component.pipe';

describe('PlayerComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
